<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./parciales/head') %>
<body>
	<!-- Header -->
	<% if(locals.user){ %>
		<% console.log('cccc' + locals.user.username) %> 
        <%- include('./parciales/headerLog', {info: locals.user}) %>
      <% } else {%>
        <%- include('./partials/header') %>
      <% }%>

	<!-- Novedades -->
	<div class="product-edit-section">
		<div class="row">
			<div class="col-12">
				<h2 class="title-edit">Editar Perfil</h2>
			</div>
        </div>
		<form class="profile-edit-form" action="/users/profile <%= info.id %> " enctype="multipart/form-data" method="POST"> 
            <div class="label-input">
                <label for="email" class="form-label">Email</label>
                <input name="email" value="<%=info.email%>" class = "border" type="email" placeholder=" example@email.com" class="email">
            </div>
            <div class="label-input">
                <label for="user" class="form-label">Usuario</label>
                <input name="username" value="<%=info.username%>"  class = "border" type="text" >
            </div>
    
            <div class="label-input">
                <label for="form-date" class="form-label">Fecha de nacimiento</label>
                <input name="date" value="<%=info.date%>" class = "border" type="date" class="form-date">
            </div>

            <div class="label-input">
                <label for="form-file" class="form-label">Foto</label>
                <input name="image" value="<%=info.image%> " type="file" class="form-file">
            </div>
			<input type="hidden" name="id" value="<%=info.id%>">
            <input type="hidden" name="createdAt" value="<%=info.createdAt%>">

            <button type="submit" class="btn btn-primary">Editar</button>
        </form>
	</div>
    
	<!-- footer -->
	<%- include('./parciales/footer') %>
	<!-- /footer -->
</body>

</html>